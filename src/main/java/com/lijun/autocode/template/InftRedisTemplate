package ${packageName};

import java.util.*;
${importList}

/**
 * 缓存接口类 I${entityName}Redis
 * @author lijun
 */
public interface I${entityName}Redis {

    /**
     * 得到redis的key值
     */
    String get${entityName}Key(${paramIds});

    /**
     * 把值存储到redis中
     * 1.Key-Value对象
     * 2.把key存储到一个set中,方便删除操作
     */
    String set${entityName}2Redis(${entityName} ${entityObj}, long expire);

    /**
     * 从缓存中得到值
     */
    ${entityName} get${entityName}ByKey(${paramIds});

    /**
     * 删除所有redis中的值
     */
    void clearAll${entityName}();
}
