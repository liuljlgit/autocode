package ${daoPackageOutPath}.inft;

import java.util.*;
import org.apache.ibatis.annotations.Param;
import org.springframework.stereotype.Repository;
import com.cloud.common.complexquery.QueryExample;
import ${entityFullPath};

/**
  * 接口类 I${entityName}Dao
  * @author lijun
  */
@Repository
public interface I${entityName}Dao {

    /**
     * 根据主键获取对象
     * @param ${entityId}
     * @return
     */
    ${entityName} load${entityName}ByKey(@Param("${entityId}") ${entityIdType} ${entityId},@Param("tablename") String tablename);

}
